<p>You previously searched for <%=@search2 %> <%= @search %>.  Click here to search for that again.</p>
<button class="btn btn-default" type="submit"><%= button_to "run that same search again", query_movie_path %></button>

<p> Enter a search below <p>

<%= form_tag "query_movie" do %>
  <input type="text" name="search" value="<%= @search %>"/>
  <select name="search_type">
    <option value="title">Title</option>
    <option value="actor">Actor</option>
  </select>
  <input type="submit" value="Go"/>
<% end %>
